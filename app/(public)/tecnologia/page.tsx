// Stub — ruta redireccionada a /procesos-tecnologias desde next.config.js.
// El redirect intercepta antes del render, así que este archivo nunca se ejecuta.
export default function Page() {
  return null
}
